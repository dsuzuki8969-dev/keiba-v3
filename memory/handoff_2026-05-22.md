# Handoff 2026-05-22 — フロントエンド大規模クリーンアップ

## セッション概要

マスター指示「4時間掛けていいから徹底的に洗い出して不要コード・バグ・未反映を調査してクリーニングと高速化と精密化」に基づく自走作業。

## コミット

| commit | 内容 |
|---|---|
| `3524911` | feat: 全ページ PremiumCard デザイン統一 — TodayPage + Calendar + Results + Database + Venue (22ファイル) |
| `90ddf6a` | refactor: フロントエンド大規模クリーンアップ — dead code 1,750行削除 + バンドル69%縮小 (88ファイル) |

**ブランチ**: `claude/hopeful-edison-85def7` (worktree)。**master マージ未実施**。

## 変更詳細

### Dead code 除去 (-1,750行)
- 未使用 UI コンポーネント 5個削除: card/dialog/select/separator/table
- `featureFlags.ts` 全削除 (FEATURE_FLAGS 未参照)
- `PaceFlowChart.tsx` (orphan) + `PaceEntry`/`buildPaceEntry` 除去
- `paraphrase.ts`: 無効化済み旧MAP辞書630行削除 (SAFE_MAP+関数は維持)
- `constants.ts`: NAR_VENUE_CODES/SURFACE_LABELS/confLevel/trendCls/weightChgCls 除去
- `tokens.ts`: typography/motion/radii 除去
- `client.ts`: shareUrl/genSimpleHtml/resultsFetch dead API 3個除去
- `hooks.ts`: usePortfolio 除去 + コメント修正
- `auth.py`: _infer_intensity dead method 除去 (36行)
- VenuePage 5タブから未使用 PremiumCardAccent import 除去
- `keibaUtils.ts`: symbolToMarkKey 除去

### バンドル最適化 (CRITICAL)
- **PastRunsTable を HorseCardPC.tsx から PastRunsTable.tsx に分離**
  - PastRunsPanel が HorseCardPC を static import → HorseCardPC の lazy() が無効化 → 555KB 巨大チャンク
  - 分離後: RaceDetailView **555KB → 174KB (69%縮小)**, HorseCardPC 14KB 独立チャンク, CartesianChart(recharts) 340KB 独立チャンク

### デザイン統一
- **hover**: 全テーブル行を `hover:bg-brand-gold/5` に統一 (BreakdownTable/AbilityTable/OddsPanel/HorseDiagnosis/DetailedAnalysis/SummaryCards — 10箇所)
- **tab**: VenueTabs/VenueDetailView/PastPredictions/DetailedAnalysis をセグメントコントロールパターンに統一 (navy gradient + gold shadow)

### 重複コード統合
- NAR_LIVE_TRACK_MAP + jraVideoTarget を constants.ts に集約 (3ファイルの重複定義除去)

### バグ修正・改善
- **ErrorBoundary 追加** (App.tsx — render エラーでの白画面クラッシュ防止)
- `dashboard.py`: ファイルハンドルリーク修正 (open().read() → with構文)
- `StatsCard.tsx`: 不要な console.info 除去
- `SummaryCards.tsx`: MP_YEARS ハードコード年度を new Date().getFullYear() ベースの動的生成に変更

## 調査レポート (未着手 — 次セッション候補)

### パフォーマンス調査結果 (HIGH priority)
- React.memo 未使用: RaceCard/HorseCardPC/HorseCardMobile 等 11コンポーネントで再レンダリング最適化可能
- HorseTable.tsx:62-71 — inline `onDMarkSelect` を useCallback 化すべき
- HomePage.tsx:109-122 — races prop で毎render新オブジェクト生成 → useMemo 化
- TodayPage.tsx:186-202 — IIFE inside JSX (computeWinPctRanks 毎render再計算)
- HomePage/ResultsPage — 一部 useQuery の error/isLoading 未ハンドリング

### アクセシビリティ調査結果 (HIGH priority)
- **CRITICAL**: ErrorBoundary 未設置 → **対応済み (今セッション)**
- HomePage div onClick (jikuList/anaHorses) — role/tabIndex/onKeyDown なし (keyboard不可)
- HorseCardPC:513 — div toggle にキーボードアクセシビリティなし
- VenueTabs/RaceDetailView/VenueDetailView — ARIA tab semantics なし
- index.html — meta description なし
- CourseExplorer/PersonnelTable — modal Escape キー未対応
- z-index: TabGroup3Horse z-30 と RaceDetailView z-20 が同一 top でスタック衝突

### 残 dead code (LOW priority)
- `shadcn` が dependencies (devDependencies に移動すべき)
- 26箇所の index-as-key (key={i}) — 大半は静的データで低リスク

## cloudflared 停止対応
- 5/22 6:33 頃マスターが本番サイト Cloudflare Tunnel Error 1033 を報告
- watchdog_check.bat は 6:35 に自動検知・再起動済み (2分間のみダウン)
- 手動でも cloudflared を再起動し復旧確認
- watchdog は正常機能中 (5分間隔チェック)

## ブランチマージ状況
- worktree ブランチ `claude/hopeful-edison-85def7` に 2 commit
- ~~**master へのマージ未実施**~~ → **5/22 朝完了** (ac8ba02 merge commit + 3b2f1b9 deploy_v3.ps1/handoff追加)
- ~~src/static は main repo にもコピー済み~~ → `src/static/` は dashboard が見ていない (使われていない遺物)。本番配信元は **`frontend/dist/`** (Flask `send_from_directory` 直配信)。CLAUDE.md「`cp -r frontend/dist/. src/static/` 同期必須」ルールは**古い仕様で要見直し** ★

## 5/22 朝 T-PERF (React 描画最適化) 完了

### 実装内容
- **React.memo 11 コンポーネント** + displayName 設定
  - HorseCardPC / HorseCardMobile / RaceCard / RaceCardOddsLine (PC+Mobile) / VenueTabs / GradeBadge / MarkBadge / ConfidenceBadge / StatsCard / BreakdownTable / AbilityTable
- **useCallback 3 箇所**: HorseTable.tsx `onDMarkSelect`, HomePage.tsx `goToRace`, TodayPage.tsx 既存 3 個は既に memoized で省略
- **useMemo 2 箇所**: HomePage.tsx `heroRaces`, TodayPage.tsx `rankMap`
- **races 参照安定化**: TodayPage.tsx に `const EMPTY_RACES: RaceSummaryItem[] = []` モジュール定数導入。`?? EMPTY_RACES` で undefined fallback → useMemo deps 毎render新参照化を防止 (typescript-reviewer HIGH#1 対応)
- build 結果: 14 ファイル / +103/-77 行 / build errors 0

### 次セッション課題 (typescript-reviewer 指摘の残件)
- **RaceCard inline onClick の useCallback 化** (HIGH#2): TodayPage.tsx:194 `onClick={() => openRace(currentVenue, r.race_no)}` で RaceCard memo 効果失効。修正には RaceCard API 変更 (raceNo prop 化) が必要でスコープ拡大のため将来課題
- **ESLint `react-refresh/only-export-components` 3 件** (HIGH#3): RaceCard.tsx (computeWinPctRanks re-export) / BreakdownTable.tsx (rankStyle / diffColor 純粋関数 export)。build は通る (vite) が `npm run lint` で exit 1。別ファイル切り出しで解消可
- **HorseCardPC 外側 wrapper の memo 未適用** (MEDIUM): 内部 memo の連鎖効果が無効化される可能性
